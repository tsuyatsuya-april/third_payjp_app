<%= link_to 'マイページへ行く', user_path(current_user) %>
<div class="content">
  <% @items.each do |item| %>
    <P>商品名<%= item.name %></p>
    <P>値段<%= item.price %>円</p>
    <%= link_to '購入する' %>
  <% end %>
</div>